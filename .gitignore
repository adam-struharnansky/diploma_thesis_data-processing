# Ignore everything
*

# Don't ignore directories, so we can include .sh files within them
!*/

# Include only .sh files
!*.sh

# py
!*.py

# Don't ignore the .gitignore file itself
!.gitignore

# Keep also the miRNA - RNA pairs
!mirna_rna_pairs.csv
